{
  "name": "aam-issue-prompt-tracker",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup:dev": "./scripts/dev-setup.sh",
    "stop:devdb": "./scripts/dev-stop.sh",
    "dev": "npm run build -w packages/shared && npm run prisma:generate -w apps/api && concurrently --kill-others-on-fail -n api,web -c blue,green \"npm run dev -w apps/api\" \"./scripts/dev-web.sh\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate -w apps/api",
    "prisma:migrate:dev": "npm run prisma:migrate:dev -w apps/api",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
