interface:
  display_name: "AAM Issue Tracker Agent"
  short_description: "Prompt-first workflows for AAM issue tracker APIs"
  default_prompt: "Use this skill to resolve tracker issues from issue.prompt context by default, then write resolution notes back to the tracker."
